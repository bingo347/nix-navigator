$sections:
  testing:
    test_substitutions: Some text with substitutions {b} and {c}
    test_plural:
      $switch: plural(count)
      one: One instance
      many: "{count} instances"
    test_reference: "Text with reference substitution: {test_plural(count)}"
    test_escape: Some {{ text } here
    test_only_in_en: Only in english locale
  test_only_in_en_section:
    value: Only in english locale
